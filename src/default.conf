server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }

  # nginx-prometheus-exporter scrapes this endpoint (localhost only)
  location /stub_status {
    stub_status;
    allow 127.0.0.1;
    deny all;
  }
}
